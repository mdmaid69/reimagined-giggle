import math
def calculate_cartesian_to_polar_coordinates(x, y):
        return math.rect(x, y)
import heapq
def create_heap(iterable):
        h = list(iterable)
        heapq.heapify(h)
        return h